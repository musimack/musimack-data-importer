# Product Owner decision packet

Status values: `BLOCKS P2-3B` means implementation must not begin until David records a choice. Recommendations in this packet are not decisions.

## Decision register

| ID       | Question                                                    | Recommended option                                                                                                  | Alternatives                                  | Advantages                                                               | Risks                                              | Reversibility / implementation impact                               | Blocker                                     |
| -------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------- |
| `PO-001` | Where should ingestion runtime live?                        | Dedicated `musimack-reporting-ingestion-prod` project plus separate dev project                                     | Existing Portal project; BigQuery project     | Clear IAM, billing, secret and blast-radius boundary                     | Cross-project IAM/operations                       | Moderately reversible; moves job/secrets/logging                    | **Yes**                                     |
| `PO-002` | What compute target?                                        | Cloud Run Jobs                                                                                                      | Dedicated VM; Portal VM                       | Bounded/serverless/manual-first, low ops, keyless identity               | Requires container/noninteractive refactor         | Reversible before scheduling; dictates entrypoint/container         | **Yes**                                     |
| `PO-003` | What ingestion boundary?                                    | Portal-owned private API                                                                                            | Direct DB; Cloud Storage/Pub/Sub              | Portal retains validation/transaction/isolation; deployment independence | New authenticated service API                      | Contract can later add queue transport; direct DB migration avoided | **Yes**                                     |
| `PO-004` | Where are canonical client/project/provider mappings?       | Portal database, versioned config API                                                                               | Importer DB/files; separate config service    | Reuses canonical UUID/access boundary and prevents duplicate mapping     | Portal admin/config schema work                    | Costly to reverse after onboarding; foundational                    | **Yes**                                     |
| `PO-005` | OAuth authorization isolation?                              | Hybrid: shared Musimack grant by provider/environment, per-client exception                                         | One universal grant; token per client         | Balances operations and least privilege                                  | Shared grant revocation/blast radius               | Binding model makes later split reversible                          | **Yes**                                     |
| `PO-006` | Secret naming/organization?                                 | Environment + provider + grant identity; stable Portal binding key; versioned Secret Manager values                 | Client-named secrets; one JSON bundle         | Auditable, rotatable, avoids names as authorization                      | More secret objects/metadata                       | Easy to adjust before production; impacts adapter/IAM               | **Yes**                                     |
| `PO-007` | Environment separation?                                     | Separate dev and prod GCP projects, service accounts, secrets, config records                                       | One project with labels                       | Strong fail-closed boundary                                              | More setup/cost administration                     | Harder to split later; do now                                       | **Yes**                                     |
| `PO-008` | Where is production Portal database and how is API reached? | Keep DB Portal-only; importer reaches authenticated HTTPS API                                                       | Importer DB access; private shared DB network | No new DB blast radius; topology independent                             | Requires reliable Portal endpoint                  | API design stable; network deployment waits on actual facts         | **Yes**                                     |
| `PO-009` | How does Portal authenticate importer?                      | Google OIDC ID token, exact audience/subject plus project authorization                                             | HMAC; mTLS; IAP-only; source IP               | Keyless, short-lived, native to Cloud Run                                | Portal must correctly verify tokens on VM          | Can add IAP/private network later                                   | **Yes**                                     |
| `PO-010` | Portal endpoint exposure?                                   | HTTPS endpoint, workload-authenticated; private/IAP if current infrastructure supports it without new fragility     | Public anonymous; VPC-only                    | Smallest deployable model with strong identity                           | Network-reachable attack surface                   | Defense-in-depth can evolve after topology inventory                | Yes, before cloud pilot                     |
| `PO-011` | Initial request/payload limits?                             | One project/provider/week; one task; 2 MiB; explicit per-provider request ceilings; zero default retries            | Multi-client batch; larger/unbounded          | Simple evidence and failure containment                                  | More manual invocations                            | Easily raised only through governed policy version                  | **Yes**, numerical ceilings required        |
| `PO-012` | Backup RPO/RTO and retention?                               | Initial RPO 24h, aspirational RTO 15m; 30 daily/12 monthly/pre-migration indefinite, subject to hosted rehearsal    | Tighter/looser objectives                     | Consistent with local measured rehearsal                                 | Not yet production-measured; storage/client policy | Policy is revisable; infrastructure/operations impact               | Blocks scheduling, not local implementation |
| `PO-013` | Client live-weekly visibility?                              | Internal/admin pilot first; later explicit client policy                                                            | Reuse all current project viewers immediately | Limits unaccepted data exposure                                          | Delays client value                                | Reversible through Portal authorization milestone                   | Blocks client pilot, not ingestion plumbing |
| `PO-014` | Coin Meter structure?                                       | One client, two projects if same customer/access boundary                                                           | Two clients; one project/two resources        | Matches Portal model and separates dashboards                            | Shared GA4 property still ambiguous                | Client split later is data migration; decide before records         | Blocks Coin Meter onboarding                |
| `PO-015` | Shared GA4 property isolation?                              | Separate properties preferred; otherwise implement accepted hostname/stream filter v1                               | Duplicate unfiltered mapping                  | Strong cross-project isolation                                           | Property change/client work or new feature         | Filter contract is additive; unfiltered duplicate is unsafe         | Blocks affected onboarding                  |
| `PO-016` | BigQuery dataset ownership?                                 | Source datasets remain in client/export projects; importer reads in place                                           | Copy to central project; shared dataset       | Preserves source authority and minimizes data copies                     | Cross-project grants/locations                     | Revisit for scale; copying later requires migration                 | Blocks P2-6 only                            |
| `PO-017` | BigQuery client isolation?                                  | Dataset/authorized-view boundary per client/project                                                                 | Shared tables with row policy                 | Understandable IAM and failure containment                               | More datasets/grants                               | Hard to repair leakage later                                        | Blocks P2-6 only                            |
| `PO-018` | BigQuery billing/ceilings?                                  | Explicit billing project; per-query/run/month bytes limits; budgets as backstop                                     | Budget only; flat reservation                 | Prevents surprise scans and supplies evidence                            | Queries can refuse during legitimate growth        | Ceiling versions are adjustable by approval                         | Blocks P2-6 only                            |
| `PO-019` | BigQuery metric boundary?                                   | GA4 API owns initial headline metrics; BigQuery only new accepted event metrics                                     | Automatically prefer BigQuery                 | Prevents conflicting dashboard truth                                     | Some duplication/reconciliation work               | Metric ownership changes require new contract/version               | Blocks P2-6 only                            |
| `PO-020` | Raw/normalized retention?                                   | No durable raw provider responses by default; Portal revisions retained under reporting policy; logs exclude values | Store raw responses in object storage         | Data minimization                                                        | Harder provider forensics without replay           | Can add approved encrypted raw evidence later                       | Yes, before contract implementation         |
| `PO-021` | When may scheduling start?                                  | Only after P2-OPS-F06, manual multi-client acceptance, alerts, recovery, ceilings, and P2-8 HAG                     | Schedule after first pilot                    | Governed unattended operation                                            | Slower automation                                  | Easy to enable later; premature use high risk                       | Scheduling blocker                          |
| `PO-022` | Future asynchronous transport trigger?                      | Stay synchronous API until measured outages/volume justify Pub/Sub/Storage                                          | Add queue now                                 | Minimum operator burden                                                  | Portal outage requires retry                       | Additive future evolution with same envelope                        | No                                          |
| `PO-023` | Secret rotation overlap?                                    | Two-version overlap with readiness/probe/manual import before disable                                               | In-place replacement                          | Safe rollback                                                            | Temporary dual valid credentials                   | Operational policy only                                             | Before production credentials               |
| `PO-024` | Google Ads authorization model/ceilings?                    | Separate P2-5 decision after account/developer-token inventory                                                      | Reuse GA4 grant assumptions                   | Avoids premature credential/query claims                                 | Defers paid integration                            | Isolated later adapter                                              | Blocks P2-5 only                            |
| `PO-025` | Deployment/IaC tool and state owner?                        | One reviewed IaC system in importer-owned deployment area; secret values excluded                                   | Console-only; separate infra repo             | Reproducibility and recovery                                             | Tool learning/state security                       | Decide before resource creation                                     | Blocks disposable cloud pilot               |

## Decisions that block the next milestone

P2-3B implementation is blocked by `PO-001` through `PO-009`, `PO-011`, and `PO-020`, plus actual sanitized production-topology facts. `PO-010` must close before the disposable cloud pilot. The remaining decisions are milestone-specific as marked.

## Required decision record format

David should record `Accept recommendation`, `Choose alternative: ...`, or `Defer` for each ID. A decision record must name date, scope/environment, constraints, and any numerical ceilings. Accepting this architecture package does not silently accept all recommendations; each unresolved ID remains open.

## Infrastructure fact packet requested from David

Provide safe metadata only:

- Portal GCP project ID/number, region/zone, runtime type, and service account name;
- Portal public hostname and reverse proxy/IAP/load-balancer shape;
- PostgreSQL location/type and whether Cloud SQL is used;
- current GCP projects for OAuth and BigQuery, dataset names/locations only when approved;
- OAuth consent mode and authorization principal policy;
- existing Secret Manager and organization-policy conventions;
- backup destination/encryption/operator model;
- logging retention and alert destination;
- approved per-provider request ceilings and cost ceilings.

Do not provide token values, client secrets, private keys, database passwords, connection strings, or raw provider payloads.
